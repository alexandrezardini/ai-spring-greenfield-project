# PostgreSQL credentials — copy this file to .env and fill in your values.
# Defaults (:-streamtube) are used when the variable is unset, so .env is optional for local dev.

POSTGRES_DB=
POSTGRES_USER=
POSTGRES_PASSWORD=
